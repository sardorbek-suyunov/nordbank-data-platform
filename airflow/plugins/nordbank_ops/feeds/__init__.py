"""The external feeds (spec 006): the modes specification 005 did not have, and the four feeds.

`identity` is file and snapshot identity by content checksum, `fetch` is the bounded retry every
interval API request goes through, and the modules beside them parse and land one feed each.
"""
