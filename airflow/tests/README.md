# airflow/tests

Tests for orchestration: DAG import integrity, DAG id and schedule assertions, and unit
tests for the operators and hooks in `airflow/plugins/`.

Tests that need a running stack are marked and excluded from the default `make test` run.

Populated from M4.
