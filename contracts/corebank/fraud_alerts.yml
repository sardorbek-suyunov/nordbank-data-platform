# Data contract for core.fraud_alerts.
#
# Bootstrapped from docs/data_dictionary.md once, then hand-authored. It is not
# regenerated on every run: the dictionary records what the source is, this records
# what the platform has agreed to accept, and the lag between them is what makes
# drift visible at ingest (spec 005 section 2).
#
# `make contracts-diff` reports divergence from the dictionary and names the
# revision this file was pinned to. Bump `contract_version` deliberately, in a
# commit, when the platform decides to accept something new.
source_system: corebank
source_schema: core
entity: fraud_alerts
contract_version: 1
watermark_column: updated_at
primary_key: fraud_alert_id
dictionary_revision: sha256:e5d088874e5b22e7
in_force_from: null
columns:
- name: fraud_alert_id
  type: bigint
  nullable: false
  classification: non-personal
- name: alert_reference
  type: character varying(40)
  nullable: false
  classification: non-personal
- name: transaction_id
  type: bigint
  nullable: false
  classification: non-personal
- name: customer_id
  type: bigint
  nullable: false
  classification: pseudonymous_key
- name: fraud_rule_code
  type: character varying(40)
  nullable: false
  classification: non-personal
- name: fraud_disposition_code
  type: character varying(40)
  nullable: false
  classification: sensitive
- name: alerted_at
  type: timestamp with time zone
  nullable: false
  classification: non-personal
- name: dispositioned_at
  type: timestamp with time zone
  nullable: true
  classification: non-personal
- name: alert_score
  type: numeric(18,8)
  nullable: false
  classification: sensitive
- name: analyst_reference
  type: character varying(40)
  nullable: true
  classification: identifier
- name: created_at
  type: timestamp with time zone
  nullable: false
  classification: non-personal
- name: updated_at
  type: timestamp with time zone
  nullable: false
  classification: non-personal
- name: is_deleted
  type: boolean
  nullable: false
  classification: non-personal
