# dbt/macros

Project macros: surrogate key hashing, money and timestamp casting, PII tokenisation and
audit column injection.

SQL lives here when two or more models need it. Single-use SQL stays in the model that uses
it.

Populated from M4.
