# dbt/snapshots

dbt snapshots capturing source-side change where the source itself keeps no history.

Snapshot output feeds the silver SCD2 models; strategy and key columns follow
`docs/conventions.md`.

Populated from M5.
