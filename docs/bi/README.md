# docs/bi

Documentation for the consumption layer: Power BI semantic model definition, measure
catalogue, report inventory and refresh configuration.

No `.pbix` binaries are stored here. The repository holds the description of the model and
the queries behind it, so that the semantic layer is reviewable as text.

Populated at M9.
