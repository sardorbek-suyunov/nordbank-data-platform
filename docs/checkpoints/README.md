# docs/checkpoints

One report per completed milestone: what was built, what was deferred and to which
milestone, known gaps, and the commands that verify the state of the repository.
Filename pattern: `M<n>-summary.md`.

A checkpoint is written when the milestone closes and is not updated afterwards. Later
corrections belong to the next checkpoint.
