# docs/diagrams

Diagram sources for the platform.

Diagrams are authored as Mermaid inside Markdown files in this directory and reviewed as
text. PNG is exported only for the diagram embedded in the root `README.md`; exports go to
`docs/diagrams/png/`. The Mermaid source stays the version of record, and an export that
disagrees with it is regenerated rather than edited.
