"""Tests for the generator.

Unit tests here need neither a database nor a running stack and are collected by `make test`.
The ones marked `integration` need the stack and are run by `make test-integration` from the
host, because that is where the Docker socket is.
"""
