# infra

Infrastructure definitions for the local stack and for the cloud sandbox.

`docker/` holds everything needed to run the platform on one machine. `terraform/` holds the
BigQuery sandbox used to prove that the dbt project runs against a second target.

No application code here.
