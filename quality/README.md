# quality

Data quality definitions: check specifications, severity levels, thresholds, and the
freshness SLA per source and layer.

This directory holds definitions only. Results are written to the `dq` schema by the `dq_*`
DAGs and reported through the gold layer (business questions 17 and 18).

Populated from M7.
